services:
  instance1: &instance
    container_name: php_instance1
    image: wendellunny/php-rinha-backend-2026:1.0
    deploy:
      resources:
        limits:
          cpus: '0.4'
          memory: 150M

  instance2:
    <<: *instance
    container_name: php_instance2
    
  load-balancer:
    container_name: nginx_load_balancer
    image: nginx:latest
    ports:
      - "9999:9999"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - instance1
      - instance2
    deploy:
      resources:
        limits:
          cpus: '0.2'
          memory: 50M
